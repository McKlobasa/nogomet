import styled from 'styled-components'

const Button = styled.button`
  background: white;
  border: none;

  height: 20px;

  margin: 3px;

  font-size: 16px;
  border-radius: 5px;
`

export default Button
